# secrets.env -- copy this file, fill in values, and keep the copy OUTSIDE any repository.
# Default path expected by bootstrap github-setup: ~/secrets.env
#
# Never commit the filled-in file.  Only this example (with empty values) is committed.

JULES_API_KEY=
PIPELINE_TOKEN=
PEOPLE_DENYLIST=
